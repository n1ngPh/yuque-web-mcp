# 通过主 MCP 移动归档一条 Table 记录

主 HTTP `/mcp` 提供 `yuque_preview_archive_table_record`。它把一条已定位记录归档到同一知识库的另一张 Table：预览之后，调用现有 `yuque_confirm_change`，服务自动完成“创建空行 → 写入业务字段 → 回读核对 → 再次检查源行 → 删除源行 → 核对两表”。客户端不必处理底层接口、剪贴板或手动分两阶段调用实验服务。

## 调用方法

1. 用 `yuque_get_table` 查看源表、目标表，向用户展示完整路径与 URL，确定要移动的**一个确切 `record_id`**。该读取返回底层记录，不应用网页的筛选、排序、分组；不能仅凭网页行号、负责人显示名或模糊文本选择要删除的记录。用 `next_offset` 继续分页。
2. 预览（不写入远端）：

```json
{
  "name": "yuque_preview_archive_table_record",
  "arguments": {
    "source_doc_url": "https://team.yuque.com/group/book/source-table",
    "target_doc_url": "https://team.yuque.com/group/book/archive-table",
    "record_id": "exact-source-record-id"
  }
}
```

多工作表时还需传 `source_sheet_id`、`target_sheet_id`。预览返回 `change_token`、`diff_digest`、过期时间、源/目标完整路径与 URL、两个固定记录 ID、字段映射与可读 Diff。目标记录 ID 在预览时生成，此后固定使用。

3. 向用户核对具体记录、字段与删除源行的授权后，确认：

```json
{
  "name": "yuque_confirm_change",
  "arguments": {
    "change_token": "preview-returned-token",
    "diff_digest": "preview-returned-digest",
    "confirm_deletions": true
  }
}
```

成功返回 `state: "succeeded"`、`source_record_id`、`target_record_id` 和核对后的记录数量。它只归档指定的一行，不会自动修改“状态”“完成时间”等业务字段。新行沿用目标表已有视图规则，不修改字段配置或视图；被目标视图筛选隐藏时，仍可通过 `yuque_get_table` 查询。

## 写入配置和支持范围

使用当前用户自己的登录会话及语雀权限。部署者必须显式配置 `WRITE_CONSISTENCY_MODE=best_effort`、`WRITE_KILL_SWITCH=false`，并在 `YUQUE_WRITE_BOOK_ALLOWLIST` 中列出精确知识库 URL（例如 `https://team.yuque.com/group/book`），按部署流程重启使配置生效。归档不会因组织空间默认开放写入而跳过精确白名单。`strict` 模式仍可预览；契约关闭、开关或白名单不满足时不会消耗确认令牌。不要让 AI 为完成业务请求擅自修改部署配置。

- 仅支持组织空间、同一知识库的两篇不同 `Table/laketable` 文档及 GRID 视图。每次只处理一行，不提供跨知识库记录归档或单次批量事务；多条记录可按下文顺序编排。源表最多 5000 行，目标在新增前须少于 5000 行，以保证复制后能完整回读。
- 支持非空 `text`、`input`、`select`、`multiSelect`、`mention`、`date`、`progress` 字段。字段优先按相同 ID 匹配且名称/类型一致，否则必须唯一同名、同类型；单选/多选按标签映射目标选项，不直接复用跨表选项 ID。
- 文本精确保留换行和空格；人员按账号 ID 核对，日期按时间核对，进度按数值核对。人员显示名、日期展示格式和系统元数据可能由语雀重新生成。
- 非空行详情正文、非空不支持字段、无法保留的单元格附加元数据、歧义/缺失映射均拒绝。目标已有业务字段完全一致的记录时拒绝重复复制，应先核对是否已归档。
- 目标获得新的记录 ID、创建/修改人和时间。原记录及字段映射保存在当前用户加密操作日志中。评论、历史版本和原系统元数据不迁移；此日志不是 `yuque_list_snapshots` 的 Doc/Sheet 一键恢复快照。

## 按条件归档多条记录

在用户明确授权的两张表和筛选条件内，先完整分页读取源表、按字段实际值筛选并固定记录 ID 清单。例如“已完成”应匹配状态选项标签，不能按任务文字猜测，也不能把网页当前视图当作全部记录。

对清单中的每条记录顺序执行：重新读取并确认仍符合条件 → 新建该条 Preview → Confirm → 立即只读对账；只有 `state=succeeded` 且 `reconciliation.observed_complete=true` 才进入下一条。每次归档都会改变两张表的记录指纹，因此不能提前生成整批 Preview 后直接依次 Confirm，也不要并发操作同一对表。筛选清单与每条原令牌、源/目标记录 ID 和结果应保留，遇到失败或不确定结果暂停后续处理。

全部完成后，重新读取两表，核对已选源记录均消失、目标字段匹配、数量变化符合成功条数，以及未选记录和目标原有记录未变。这是客户端对单行工具的顺序编排，不是具有整体回滚能力的批量事务。

2026-09-20 在前述测试副本中继续按“状态=已完成”筛选 12 条记录，逐条通过主 MCP 归档并立即对账，全部 `succeeded`。源副本 46 → 34 行，目标副本 229 → 241 行；每条 15–16 个映射业务字段最终匹配，263 条其他副本记录未变，两张原表全部记录未变。测试副本保留。

## 中断与对账

创建、写字段、删除源行之前，先将操作阶段保存到本地加密日志。非幂等写入不自动重试，确认令牌只执行一次；进程重启会把未完成执行标记为 `unknown`，保留最后保存的阶段和记录 ID。

收到 `partial`、`unknown` 或客户端超时后，使用**原令牌**调用：

```json
{
  "name": "yuque_get_table_archive_status",
  "arguments": {
    "change_token": "original-preview-token",
    "reconcile": true
  }
}
```

不传 `reconcile` 时只读本地状态，不要求远端登录；设为 `true` 时使用当前会话只读查询两张表。返回源行是否存在/是否变化、目标字段是否匹配、行详情是否为空、其他行是否保持原状，以及 `observed_complete`。预览过期后仍可用原令牌查询日志。对账不重写状态、不重试写入、不自动回滚。

| 返回状态 | 含义与处理 |
| --- | --- |
| `conflict` | 本次在发送首个写请求前发现不一致或校验失败；重新读取数据并核对原因。 |
| `partial` | 已尝试复制，但后续校验或本地记录失败；可能留下空行或已填充的副本。先只读对账。 |
| `unknown` | 写入结果不确定、删除后未完成核对，或服务中断。先只读对账，不能根据超时推断写入未发生。 |
| `succeeded` | 本次观察到源行已移除、目标业务字段匹配，且其他行通过指纹核对。 |

对账时 `observed_complete=true` 表示当前观察到归档完成；历史 `state` 仍可能为 `unknown`。不要因此重新确认旧令牌。若源行仍在、目标已经复制，也不要新建预览或换记录 ID 盲目重试；由操作人员核对两个固定记录 ID 后处理。当前未提供自动恢复或回滚写入工具。

## 一致性与验证边界

语雀这些接口没有已验证的跨表事务或原子 CAS。服务在写入前及删除前重新读取源行、字段和其他行指纹，在复制后及删除后验证目标，且在同一用户的服务实例内按固定顺序锁定两张表。这能发现已观察到的变化，仍不能排除最后一次读取与删除之间其他客户端编辑的竞争窗口；其他用户的实例和网页编辑不会被本地锁阻塞。操作期间应协调停止编辑相关记录。`strict` 因此保持只预览。

底层请求格式来自浏览器实际捕获；此前独立实验曾执行授权单行归档。本次主 MCP 集成及完整字段映射通过合成数据、回环 HTTP 上游、真实客户端传输和加密数据库测试，覆盖复制校验失败、源行变化、超时不重试、确认门禁和重启对账。2026-09-20 又通过本地主 HTTP MCP 对两张独立副本完成真实验收：源副本 47 → 46 行，目标副本 228 → 229 行；14 个非空业务字段、6 种类型匹配，其余 274 条副本记录未变，两张原表合计 275 条记录保持原状。重复确认被拒绝，状态只读对账确认完成。`input` 字段及异常路径仍以离线用例验证。本地验收不等于远端服务已经升级。

旧 [独立 stdio 实验](experiments/table-archive/README.md) 保留四个 `table_archive_*` 工具和两字段/本人记录限制。新接入优先使用本文主 MCP 方法，无需单独生成计划文件或连接实验进程。
