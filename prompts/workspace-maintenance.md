# Workspace maintenance

Use the `yuque-workspace` workflow to perform a knowledge-base, directory, comment, version, collaborator, or whole-object maintenance task. Check capability status and deployment safeguards first. Always display the complete path and URL. For permission changes show the collaborator and role transition. For deletion show the exact deletion effect and recovery limitation, require explicit Preview approval, and never bypass a disabled capability.
