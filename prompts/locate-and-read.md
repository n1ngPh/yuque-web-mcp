# Locate and read

Use the `yuque-workspace` workflow. Find the requested Yuque item by title or path without loading unrelated full document bodies. Show every matching complete path and URL if the name is ambiguous. After the user selects one target, read only the required document segment or Sheet range, state the selected full path and URL first, and then answer with source content clearly separated from inference.
